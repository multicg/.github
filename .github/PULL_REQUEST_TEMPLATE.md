<!--
PR 템플릿 (기본). 유형별 템플릿(버그 수정/기능 추가/되돌리기)이 필요하면
PR 생성 화면에서 "Preview" 옆 링크나 URL에 `?template=bug_fix.md` /
`?template=feature.md` / `?template=revert.md`를 붙여 선택한다.

작성 후 이 주석과 각 절의 안내 주석은 지운다. 해당 없는 선택 절은
절째로 지운다. API/CLI로 PR을 만들 때는 템플릿이 자동 적용되지
않으므로 이 구조를 직접 따른다.
-->

## 요약

<!-- 무엇이 바뀌었는가. 파일·기능 단위 불릿으로, 리뷰어가 diff를 열기 전에 범위를 잡을 수 있게 쓴다. -->

-

## 배경

<!-- 선택. 왜 이 변경이 필요했는가. 이슈에 이미 적혀 있으면 이 절을 지우고 이슈 링크로 대신한다. -->

## 검증

<!-- 무엇을 어떻게 확인했는가. 실행한 명령과 그 출력을 함께 적는다.
     "통과"가 아니라 "몇 건을 검사해서 통과"까지 쓴다. -->

- `npx markdownlint-cli2 "**/*.md"` → `Linting: N files`, 0 issues

## 영향

<!-- 선택. 기존 동작·인터페이스를 바꿨으면 무엇이 어떻게 달라지는가. 새로 추가만 했고 기존 것을 건드리지 않았으면 이 절을 지운다. -->

## 범위

<!-- 선택. 이 PR이 다루지 않은 것과 그 이유. 후속 PR로 넘겼으면 그렇게 적는다. 없으면 이 절을 지운다. -->

## 체크리스트

- [ ] 구조 변경(문서·디렉터리 추가, 지침 파일·CI 잡·워크플로 규칙·도구 스크립트의 추가·변경)이면 `README.md`를 같은 PR에서 갱신했다
- [ ] markdownlint CI가 통과했다
- [ ] 아래 이슈 연결을 채웠다

## 이슈 연결

<!-- 이 PR로 이슈가 끝나면 `Closes #<이슈번호>`, 후속 PR이 남아 있으면 `Refs #<이슈번호>`. -->

Closes #
Refs #
