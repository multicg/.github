<!-- PR 템플릿 (bug_fix). 공통부는 PULL_REQUEST_TEMPLATE.md 참고. -->

## 요약

<!-- 무엇이 바뀌었는가. 파일·기능 단위 불릿으로, 리뷰어가 diff를 열기 전에 범위를 잡을 수 있게 쓴다. -->

-

## 배경

<!-- 선택. 왜 이 변경이 필요했는가. 이슈에 이미 적혀 있으면 이 절을 지우고 이슈 링크로 대신한다. -->

## 검증

<!-- 무엇을 어떻게 확인했는가. 실행한 명령과 그 출력을 함께 적는다. -->

- `npx markdownlint-cli2 "**/*.md"` → `Linting: N files`, 0 issues

## 영향

<!-- 선택. 기존 동작·인터페이스를 바꿨으면 무엇이 어떻게 달라지는가. -->

## 범위

<!-- 선택. 이 PR이 다루지 않은 것과 그 이유. -->

## 재현

<!-- 고치기 전에 무엇이 어떻게 잘못 동작했는가. 명령·입력·관찰된 출력을 적고, 기대했던 출력과 나란히 둔다. -->

```text
실행:
관찰:
기대:
```

## 원인

<!-- 왜 그렇게 동작했는가. 증상이 아니라 원인을 적는다 — "무엇이 잘못된 가정이었나". -->

## 재발 방지

<!-- 같은 원인이 다시 들어오는 것을 무엇이 막는가. 아무것도 추가하지 않았으면 그 이유를 적는다. -->

## 체크리스트

- [ ] 구조 변경이면 `README.md`를 같은 PR에서 갱신했다
- [ ] markdownlint CI가 통과했다
- [ ] 아래 이슈 연결을 채웠다
- [ ] 수정 전 재현 절차를 적었고, 수정 후 같은 절차로 재현되지 않음을 확인했다
- [ ] 증상이 아니라 원인을 적었다(특정 못했으면 그렇게 적었다)
- [ ] 재발 방지 수단을 추가했거나, 추가하지 않은 이유를 적었다

## 이슈 연결

Closes #
Refs #
