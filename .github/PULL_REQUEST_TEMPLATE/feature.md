<!-- PR 템플릿 (feature). 공통부는 PULL_REQUEST_TEMPLATE.md 참고. -->

## 요약

<!-- 무엇이 바뀌었는가. 파일·기능 단위 불릿으로, 리뷰어가 diff를 열기 전에 범위를 잡을 수 있게 쓴다. -->

-

## 배경

<!-- 선택. 왜 이 변경이 필요했는가. 이슈에 이미 적혀 있으면 이 절을 지우고 이슈 링크로 대신한다. -->

## 검증

<!-- 무엇을 어떻게 확인했는가. 실행한 명령과 그 출력을 함께 적는다. -->

- `npx markdownlint-cli2 "**/*.md"` → `Linting: N files`, 0 issues

## 영향

<!-- 선택. 기존 동작·인터페이스를 바꿨으면 무엇이 어떻게 달라지는가. -->

## 범위

<!-- 선택. 이 PR이 다루지 않은 것과 그 이유. -->

## 인터페이스

<!-- 이 기능을 쓰는 쪽에서 무엇이 보이는가. 실제로 실행해 본 예시를 붙인다. -->

```bash

```

## 호환성

<!-- 기존 방식이 그대로 동작하는가. 새로 추가만 했고 기존 것을 건드리지 않았으면 "기존 동작 변경 없음"이라고 적는다. -->

## 대안

<!-- 선택. 다른 방식을 검토했다면 무엇을 왜 택하지 않았는가. -->

## 체크리스트

- [ ] 구조 변경이면 `README.md`를 같은 PR에서 갱신했다
- [ ] markdownlint CI가 통과했다
- [ ] 아래 이슈 연결을 채웠다
- [ ] 실행해 본 사용 예시를 넣었다(설명만으로 대신하지 않았다)
- [ ] 기존 동작이 바뀌면 쓰던 쪽(문서·스크립트·절차)을 같은 PR에서 함께 고쳤다

## 이슈 연결

Closes #
Refs #
